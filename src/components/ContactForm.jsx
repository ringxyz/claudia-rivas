import React,{useState,useRef} from 'react';
import {email} from '../site';
export default function ContactForm({en}){const [status,setStatus]=useState(''),[busy,setBusy]=useState(false);const pending=useRef(false);const endpoint=import.meta.env.VITE_CONTACT_ENDPOINT==='mailto'?'':(import.meta.env.VITE_CONTACT_ENDPOINT||'https://formsubmit.co/ajax/enterprisesbusinesscorp@gmail.com');async function submit(e){e.preventDefault();if(pending.current)return;const data=new FormData(e.currentTarget);if(data.get('website'))return;const payload=Object.fromEntries(data);delete payload.website;if(!endpoint){const subject=`${en?'Professional proposal':'Propuesta profesional'} — ${payload.name}`;const body=`${payload.message}\n\n${payload.name}\n${payload.email}\n${payload.company||''}`;window.location.href=`mailto:${email}?subject=${encodeURIComponent(subject)}&body=${encodeURIComponent(body)}`;setStatus(en?'Your email app will open a draft. Review it and send it there.':'Tu aplicación de correo abrirá un borrador. Revísalo y envíalo desde allí.');return;}pending.current=true;setBusy(true);setStatus('');try{const response=await fetch(endpoint,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)});const result=await response.json();if(!response.ok||!(result.success===true||result.success==='true'))throw Error('delivery');setStatus(en?'The service has accepted your proposal. Thank you.':'El servicio ha aceptado tu propuesta. Gracias.');e.target.reset();}catch{setStatus(en?'We could not submit your message. Please email us directly.':'No se pudo enviar el mensaje. Escríbenos directamente por correo.');}finally{pending.current=false;setBusy(false);}}return <form className="contact-form" onSubmit={submit}><div className="form-row"><label>{en?'Name':'Nombre'}<input name="name" autoComplete="name" required maxLength="120"/></label><label>{en?'Email':'Correo electrónico'}<input name="email" type="email" autoComplete="email" required maxLength="254"/></label></div><label>{en?'Company / project (optional)':'Empresa / proyecto (opcional)'}<input name="company" autoComplete="organization" maxLength="180"/></label><label>{en?'Your proposal':'Tu propuesta'}<textarea name="message" required rows="4" maxLength="5000"/></label><div className="honeypot" aria-hidden="true"><label>Website<input name="website" tabIndex="-1" autoComplete="off"/></label></div><p className="form-note">{endpoint?(en?'Your information is processed through FormSubmit to respond to your proposal.':'Tus datos se procesan mediante FormSubmit para responder a tu propuesta.'):(en?'This form prepares an email draft in your email app. It does not send automatically.':'Este formulario prepara un borrador en tu aplicación de correo. No lo envía automáticamente.')}</p><button className="submit-button" disabled={busy}>{busy?(en?'Sending…':'Enviando…'):endpoint?(en?'Send proposal':'Enviar propuesta'):(en?'Open in my email':'Abrir en mi correo')} ↗</button><p role="status" aria-live="polite">{status}</p></form>;}


